CREATE TABLE QNA_TBL(
	QNA_NO NUMBER PRIMARY KEY, 
	USER_ID VARCHAR2(20) CONSTRAINT FK_USER_ID REFERENCES USER(USER_ID) ON DELTET CASCADE, (컬럼 레벨)
	REPLY_STATE VARCHAR2(2) NOT NULL DEFAULT 'N',
	QNA_TITLE VARCHAR2(200) NOT NULL,
	QNA_CONTENT VARCHAR2(1000) NOT NULL,
	QNA_WRITEDATE DATE DEFAULT SYSDATE,
	REPLY_CONTENT VARCHAR2(1000),
	REPLY_WRITEDATE DATE DEFAULT SYSDATE,
	REPLY_USERID VARCHAR2(20) 
	--FOREIGN KEY(USER_ID) REFERENCES USER(USER_ID) ON DELETE CASCADE(테이블 레벨)
);

CREATE TABLE QNA_TBL(
	QNA_NO NUMBER,  
	USER_ID VARCHAR2(20), 
	REPLY_STATE VARCHAR2(2) DEFAULT 'N',
	QNA_TITLE VARCHAR2(200),
	QNA_CONTENT VARCHAR2(1000), 
	QNA_WRITEDATE DEFAULT SYSDATE,
	REPLY_CONTENT VARCHAR2(1000),
	REPLY_WRITEDATE DEFAULT SYSDATE,
	REPLY_USERID VARCHAR2(20) 
);

--제약 조건 추가하기
ALTER TABLE QNA_TBL ADD PRIMARY_KEY("QNA_NO");
ALTER TABLE QNA_TBL MODIFY QNA_TITLE NOT NULL;
ALTER TABLE QNA_TBL MODIFY QNA_CONTENT NOT NULL;
ALTER TABLE QNA_TBL MODIFY REPLY_STATE NOT NULL;
ALTER TABLE QNA_TBL ADD CONSTRAINT FK_USER_ID FOREIGN KEY(USER_ID) REFERENCES(USER_ID) ON DELETE CASCADE;


-- USERS 테이블에 khuser01이란 아이디가 존재함.
-- QNA 테이블에 데이터를 넣어보시오
INSERT INTO QNA_TBL VALUES(1, 'khuser01', DEFAULT, 'title good', 'contents good', DEFAULT, 
'reply contents', DEFAULT, 'admin');




1. DDL
- 제약조건 
2. DML
- DQL, JOIN SET OPERATOR, SubQuery
3. DCL
4. TCL 
5. Oracle Object
- VIEW (Data Dictionary - Data Dictionary View)
- SEQUENCE



이클립스 공부할때 member 연결하는 클래스 확인할 것.
JDBC-----------------------------

1. DBMS
2. JDBC
- 클래스 / 인터페이스
- COMMIT/ROLLBACK
- 자원해제 : 내가 실행한 메소드에서 계속 자원을 잡고 있게된다. 동작에는 문제가 없다. 
- 연결, 싱글톤 패턴이 뭔지 왜 사용하는지 쓰면 뭐가 좋은지
- Statement vs PreparedStatement
3. SQL : SELECT, INSERT, UPDATE, DELETE


















